import { SectionHeading } from './section-heading';
const steps = [['01', 'Understand', 'Clarify the problem, people and requirements before choosing a solution.'], ['02', 'Design', 'Shape the architecture and user experience into a clear direction.'], ['03', 'Build', 'Develop maintainable frontend and backend systems with care.'], ['04', 'Improve', 'Test, optimize, refine and prepare the product to ship.']];
export function Process() { return <section className="section process"><div className="shell"><SectionHeading eyebrow="03 — HOW I WORK" title="A considered path from problem to product."/><div className="process-grid">{steps.map(([number, title, copy]) => <div className="process-step" key={number}><span>{number}</span><h3>{title}</h3><p>{copy}</p></div>)}</div></div></section>; }
